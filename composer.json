{
    "name": "mrf/synchro",
    "type": "typo3-flow-package",
    "description": "Flow (Neos) package, which gives a command to synchronize database and resources with remote instances in both directions (push and pull). Useful for fast update of the project on development context or fast upload of the changes remotely, when working on multiple instances (e.g. live, beta, local).",
    "license": "MIT",
    "require": {
        "typo3/flow": "*",
        "typo3/surf": "0.9.*"
    },
    "autoload": {
        "psr-0": {
            "Mrf\\Synchro": "Classes"
        }
    }
}